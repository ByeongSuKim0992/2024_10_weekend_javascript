// 기본 타입: string,number,boolean,undefined,null
// 참조 타입:
// 내장타입(=객체):Array, Object, Math, Date
// HTML관련:Element, Docuemnt, Event

const x = 242.123;

Math.round(x); // x 반올림
Math.floor(x); // x 내림
Math.ceil(x); // x 올림
Math.trunc(x); // x를 소수부분 제거하여 정수 반환

Math.abs(x); // x를 절댓값 반환
Math.max(a, b, c); // a,b,c 제일 큰 숫자 돌려주기
Math.min(a, b, c); // a,b,c 제일 작은 숫자 돌려주기
Math.pow(x, y); // x의 y제곱 -> x ** y
